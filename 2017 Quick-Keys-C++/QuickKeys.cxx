#include <sstream>

#include "quickkeys.hxx"
#include "uinputlib.h"

int main(int argc, char **argv)
{
    /*QuickKeys *qk = new QuickKeys();
    std::cout << qk->port << std::endl;
    mainScript(qk);
    delete(qk);*/
    test();
    //Profile *p = new Profile();
    //std::cout << p->name << std::endl;
}

