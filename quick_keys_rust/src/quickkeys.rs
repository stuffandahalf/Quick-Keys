use serialport::SerialPort;
use std::string::String;

use profile::Profile;

//#[derive(Debug, Clone, Copy]
pub struct QuickKeys {
    profile: Profile,
    port: SerialPort,
}

impl QuickKeys {
    
}
